# ********RoostGPT********

# Test generated by RoostGPT for test roost_test using AI Type Azure Open AI and AI Model gpt-4o
# 
# Test file generated for /user/logout_get for http method type GET 
# RoostTestHash=1092b8f68f
# 
# 

# ********RoostGPT********
# test_user_logout.py

import pytest
import requests
import yaml

# Load configuration from config.yaml
@pytest.fixture(scope="module")
def config():
    with open("config.yaml", "r") as f:
        return yaml.safe_load(f)

# Base URL Fixture
@pytest.fixture(scope="module")
def base_url(config):
    return config["api"]["host"]

# Authentication Headers Fixture
@pytest.fixture(scope="module")
def headers(config):
    return {
        "Authorization": f"Bearer {config['authentication']['petstore_auth']}",
        "x-api-key": config["authentication"]["api_key"]
    }

# Test User Logout Endpoint
@pytest.mark.parametrize("expected_status_code", [200, 500])
def test_user_logout(base_url, headers, expected_status_code):
    """
    Tests '/user/logout' endpoint for both success and error cases.
    """
    endpoint = f"{base_url}/user/logout"
    
    if expected_status_code == 200:
        # Test successful logout
        response = requests.get(endpoint, headers=headers)
        assert response.status_code == 200, "Expected 200 status code"
        assert response.text == "successful operation", "Expected 'successful operation' response body."
    else:
        # Test unexpected error scenario
        # Simulating an error case by sending incorrect headers
        invalid_headers = {"Authorization": "Bearer invalid_token"}
        response = requests.get(endpoint, headers=invalid_headers)
        assert response.status_code == 500, "Expected 500 status code"
        assert "Unexpected error" in response.text, "Expected response with error description."

# Test for Missing Authentication
def test_user_logout_missing_auth(base_url):
    """
    Tests '/user/logout' endpoint when authentication is missing.
    """
    endpoint = f"{base_url}/user/logout"
    response = requests.get(endpoint)
    assert response.status_code == 401, "Expected 401 status code for missing authentication"

# Instructions for Run:
# Save this file as test_user_logout.py.
# Ensure config.yaml is present in the same directory.
# Run tests using pytest: `pytest test_user_logout.py`
