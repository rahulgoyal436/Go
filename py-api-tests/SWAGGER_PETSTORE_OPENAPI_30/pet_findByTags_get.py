# ********RoostGPT********

# Test generated by RoostGPT for test roost_test using AI Type Azure Open AI and AI Model gpt-4o
# 
# Test file generated for /pet/findByTags_get for http method type GET 
# RoostTestHash=288991d4ff
# 
# 

# ********RoostGPT********
import pytest
from jsonschema import validate

# Configurable URL endpoint path
ENDPOINT = "/pet/findByTags"

# Expected schema of the `200` response
RESPONSE_SCHEMA_200 = {
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "id": {"type": "integer", "format": "int64"},
            "name": {"type": "string"},
            "category": {
                "type": "object",
                "properties": {
                    "id": {"type": "integer", "format": "int64"},
                    "name": {"type": "string"}
                }
            },
            "photoUrls": {
                "type": "array",
                "items": {"type": "string"}
            },
            "tags": {
                "type": "array",
                "items": {
                    "type": "object",
                    "properties": {
                        "id": {"type": "integer", "format": "int64"},
                        "name": {"type": "string"}
                    }
                }
            },
            "status": {"type": "string"}
        },
        "required": ["name", "photoUrls"]
    }
}

# Test for successful response (HTTP 200)
@pytest.mark.parametrize(
    "tags, expected_status",
    [
        (["dog", "cat"], 200),
        (["fish"], 200),
        ([], 200)  # Empty tags should still work
    ]
)
def test_find_by_tags_success(api_client, valid_api_key, tags, expected_status):
    """Verify successful operation with valid tags"""
    headers = {"Authorization": f"Bearer {valid_api_key}"}
    params = {"tags": tags}

    response = api_client.get(ENDPOINT, headers=headers, params=params)

    assert response.status_code == expected_status, f"Unexpected status code: {response.status_code}"
    response_data = response.json()

    # Validate the response schema
    validate(instance=response_data, schema=RESPONSE_SCHEMA_200)

# Test for invalid API key
def test_find_by_tags_invalid_api_key(api_client, invalid_api_key):
    """Verify 403 response with an invalid API key"""
    headers = {"Authorization": f"Bearer {invalid_api_key}"}
    params = {"tags": ["dog", "cat"]}

    response = api_client.get(ENDPOINT, headers=headers, params=params)

    assert response.status_code == 403, f"Unexpected status code: {response.status_code}"

# Test for missing authentication
def test_find_by_tags_missing_auth(api_client):
    """Verify 401 response when authentication is missing"""
    params = {"tags": ["dog", "cat"]}

    response = api_client.get(ENDPOINT, headers=None, params=params)

    assert response.status_code == 401, f"Unexpected status code: {response.status_code}"

# Test for client errors (HTTP 400)
@pytest.mark.parametrize(
    "tags, expected_status",
    [
        ([""], 400),  # Invalid format (empty string in array)
        (None, 400)   # Provide None value for tags
    ]
)
def test_find_by_tags_invalid_tags(api_client, valid_api_key, tags, expected_status):
    """Verify client error handling for invalid tag values"""
    headers = {"Authorization": f"Bearer {valid_api_key}"}
    params = {"tags": tags}

    response = api_client.get(ENDPOINT, headers=headers, params=params)

    assert response.status_code == expected_status, f"Unexpected status code: {response.status_code}"

# Test for server errors (default response)
def test_find_by_tags_unexpected_error(api_client, valid_api_key):
    """Simulate an unexpected error response"""
    headers = {"Authorization": f"Bearer {valid_api_key}"}
    params = {"tags": ["simulate-error"]}  # Assume this tag causes a server error

    response = api_client.get(ENDPOINT, headers=headers, params=params)

    assert response.status_code == 500, f"Unexpected status code: {response.status_code}"
