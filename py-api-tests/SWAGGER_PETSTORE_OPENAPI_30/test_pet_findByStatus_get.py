# ********RoostGPT********

# Test generated by RoostGPT for test roost_test using AI Type Azure Open AI and AI Model gpt-4o
# 
# Test file generated for /pet/findByStatus_get for http method type GET 
# RoostTestHash=d31df25c68
# 
# 

# ********RoostGPT********
import pytest


@pytest.mark.parametrize("status", [
    "available",    # Valid status value
    "pending",      # Valid status value
    "sold",         # Valid status value
    None            # Default status value (optional)
])
def test_find_by_status_valid(api_client, config, status):
    """
    Test the /pet/findByStatus endpoint with valid status values.
    """
    headers = {"Authorization": f"Bearer {config['authentication']['petstore_auth']}"}
    params = {"status": status} if status is not None else {}

    response = api_client.get("/pet/findByStatus", headers=headers, params=params)
    
    assert response.status_code == 200, f"Expected 200, got {response.status_code}"
    assert isinstance(response.json(), list), "Response should be a list of pets"
    
    for pet in response.json():
        assert "name" in pet and pet["name"], "Pet name is required"
        assert "photoUrls" in pet and isinstance(pet["photoUrls"], list), "Photo URLs are required"
        assert "id" in pet


@pytest.mark.parametrize("status", [
    "INVALID_STATUS",  # Invalid status value
])
def test_find_by_status_invalid(api_client, config, status):
    """
    Test the /pet/findByStatus endpoint with invalid status values.
    """
    headers = {"Authorization": f"Bearer {config['authentication']['petstore_auth']}"}
    params = {"status": status}
    
    response = api_client.get("/pet/findByStatus", headers=headers, params=params)
    
    assert response.status_code == 400, f"Expected 400, got {response.status_code}"
    assert "Invalid status value" in response.text, "Response should indicate an invalid status"


def test_find_by_status_without_auth(api_client):
    """
    Test the /pet/findByStatus endpoint without authentication.
    """
    params = {"status": "available"}
    
    response = api_client.get("/pet/findByStatus", params=params)
    
    assert response.status_code == 401, f"Expected 401, got {response.status_code}"


def test_find_by_status_expired_token(api_client, config):
    """
    Test the /pet/findByStatus endpoint with expired token.
    """
    expired_token = "EXPIRED_TOKEN"
    headers = {"Authorization": f"Bearer {expired_token}"}
    params = {"status": "available"}
    
    response = api_client.get("/pet/findByStatus", headers=headers, params=params)
    
    assert response.status_code == 401, f"Expected 401, got {response.status_code}"


def test_find_by_status_schema_validation(api_client, config, schema_validator):
    """
    Validate response schema for /pet/findByStatus.
    """
    headers = {"Authorization": f"Bearer {config['authentication']['petstore_auth']}"}
    params = {"status": "available"}
    
    response = api_client.get("/pet/findByStatus", headers=headers, params=params)
    
    assert response.status_code == 200, f"Expected 200, got {response.status_code}"
    
    for pet in response.json():
        assert schema_validator.validate_pet_schema(pet), f"Schema validation failed for pet: {pet}"
