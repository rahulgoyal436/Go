# ********RoostGPT********

# Test generated by RoostGPT for test roost_test using AI Type Azure Open AI and AI Model gpt-4o
# 
# Test file generated for /pet/{petId}_get for http method type GET 
# RoostTestHash=19a7d06811
# 
# 

# ********RoostGPT********
import pytest
from jsonschema import validate

@pytest.mark.parametrize("pet_id", [1, 100, 9999])
def test_get_pet_by_id_success(api_client, valid_api_key, pet_id):
    """
    Test /pet/{petId} endpoint for successful operation (200).
    Ensures the response matches the schema and includes required properties.
    """
    headers = {"Authorization": valid_api_key}
    response = api_client.get(f"pet/{pet_id}", headers=headers)

    assert response.status_code == 200, f"Expected 200 but got {response.status_code}"

    response_data = response.json()
    expected_schema = {
        "type": "object",
        "properties": {
            "id": {"type": "integer"},
            "name": {"type": "string"},
            "category": {
                "type": "object",
                "properties": {
                    "id": {"type": "integer"},
                    "name": {"type": "string"}
                }
            },
            "photoUrls": {"type": "array", "items": {"type": "string"}},
            "tags": {
                "type": "array",
                "items": {"type": "object", "properties": {"id": {"type": "integer"}, "name": {"type": "string"}}}
            },
            "status": {"type": "string"}
        },
        "required": ["name", "photoUrls"]
    }
    # Validate response schema
    validate(instance=response_data, schema=expected_schema)

@pytest.mark.parametrize("invalid_pet_id", [-1, 0, "abc"])
def test_get_pet_by_id_invalid_id(api_client, valid_api_key, invalid_pet_id):
    """
    Test /pet/{petId} endpoint with invalid ID, expecting a 400 status code.
    """
    headers = {"Authorization": valid_api_key}
    response = api_client.get(f"pet/{invalid_pet_id}", headers=headers)

    assert response.status_code == 400, f"Expected 400 but got {response.status_code}"

@pytest.mark.parametrize("pet_id", [12345])
def test_get_pet_by_id_not_found(api_client, valid_api_key, pet_id):
    """
    Test /pet/{petId} endpoint with a non-existent ID, expecting a 404 status code.
    """
    headers = {"Authorization": valid_api_key}
    response = api_client.get(f"pet/{pet_id}", headers=headers)

    assert response.status_code == 404, f"Expected 404 but got {response.status_code}"

@pytest.mark.parametrize("pet_id", [1])
def test_get_pet_by_id_missing_auth(api_client, pet_id):
    """
    Test /pet/{petId} endpoint without authentication, expecting a 401 or failure.
    """
    response = api_client.get(f"pet/{pet_id}")

    assert response.status_code in (401, 403), f"Expected 401 or 403 but got {response.status_code}"

@pytest.mark.parametrize("pet_id", [1])
def test_get_pet_by_id_invalid_api_key(api_client, invalid_api_key, pet_id):
    """
    Test /pet/{petId} endpoint with an invalid API key, expecting a 403 status code.
    """
    headers = {"Authorization": invalid_api_key}
    response = api_client.get(f"pet/{pet_id}", headers=headers)

    assert response.status_code == 403, f"Expected 403 but got {response.status_code}"
