# ********RoostGPT********

# Test generated by RoostGPT for test roost_test using AI Type Azure Open AI and AI Model gpt-4o
# 
# Test file generated for /pet/{petId}_get for http method type GET 
# RoostTestHash=19a7d06811
# 
# 

# ********RoostGPT********
# test_api.py

import pytest
import json
from jsonschema import validate, ValidationError

@pytest.mark.parametrize("api_key", [pytest.lazy_fixture("valid_api_key"), pytest.lazy_fixture("invalid_api_key")])
def test_get_pet_by_id(api_client, api_key):
    """
    Test endpoint /pet/{petId} with valid and invalid API keys.
    """
    endpoint = "pet/10"  # Example petId
    headers = {"Authorization": f"Bearer {api_key}"}
    response = api_client.get(endpoint, headers=headers)

    # Test responses
    if api_key == pytest.lazy_fixture("valid_api_key"):
        assert response.status_code == 200, "Expected HTTP 200 for valid API key"
        validate_pet_schema(response.json())
    else:
        assert response.status_code in [401, 403], "Expected HTTP 401 or 403 for invalid API key"

@pytest.mark.parametrize("oauth_token", [pytest.lazy_fixture("oauth2_token"), "invalid_token"])
def test_get_pet_by_id_with_oauth2(api_client, oauth_token):
    """
    Test endpoint /pet/{petId} with valid and invalid OAuth2 tokens.
    """
    endpoint = "pet/10"  # Example petId
    headers = {"Authorization": f"Bearer {oauth_token}"}
    response = api_client.get(endpoint, headers=headers)

    # Test responses
    if oauth_token == pytest.lazy_fixture("oauth2_token"):
        assert response.status_code == 200, "Expected HTTP 200 for valid OAuth2 token"
        validate_pet_schema(response.json())
    else:
        assert response.status_code in [401, 403], "Expected HTTP 401 or 403 for invalid token"

@pytest.mark.parametrize("pet_id, expected_status", [
    (10, 200),  # Valid petId
    ("invalid", 400),  # Invalid petId format
    (99999, 404),  # Nonexistent petId
])
def test_get_pet_by_id_response_codes(api_client, valid_api_key, pet_id, expected_status):
    """
    Test endpoint /pet/{petId} for various response codes.
    """
    endpoint = f"pet/{pet_id}"
    headers = {"Authorization": f"Bearer {valid_api_key}"}
    response = api_client.get(endpoint, headers=headers)
    assert response.status_code == expected_status, f"Expected HTTP {expected_status} for petId {pet_id}"

def validate_pet_schema(data):
    """
    Validate the response schema for /pet/{petId}.
    """
    schema = {
        "type": "object",
        "required": ["id", "name", "photoUrls"],
        "properties": {
            "id": {"type": "integer"},
            "name": {"type": "string"},
            "category": {
                "type": "object",
                "properties": {
                    "id": {"type": "integer"},
                    "name": {"type": "string"}
                }
            },
            "photoUrls": {
                "type": "array",
                "items": {"type": "string"}
            },
            "tags": {
                "type": "array",
                "items": {
                    "type": "object",
                    "properties": {
                        "id": {"type": "integer"},
                        "name": {"type": "string"}
                    }
                }
            },
            "status": {"type": "string"}
        }
    }
    try:
        validate(instance=data, schema=schema)
    except ValidationError as e:
        pytest.fail(f"Schema validation failed: {e}")

