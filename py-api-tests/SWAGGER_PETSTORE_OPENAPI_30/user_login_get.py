# ********RoostGPT********

# Test generated by RoostGPT for test roost_test using AI Type Azure Open AI and AI Model gpt-4o
# 
# Test file generated for /user/login_get for http method type GET 
# RoostTestHash=fbb4064353
# 
# 

# ********RoostGPT********
# test_user_login.py

import pytest

# Test data for parameterized tests
valid_credentials = [
    {"username": "user1", "password": "password1"},
    {"username": "user2", "password": "password2"}
]

invalid_credentials = [
    {"username": "invalid_user", "password": "wrong_password"},
    {"username": "", "password": "no_username"}
]

@pytest.mark.parametrize("credentials", valid_credentials)
def test_login_success(api_client, credentials):
    """
    Test successful login with valid credentials.
    """
    params = {"username": credentials['username'], "password": credentials['password']}
    response = api_client.get("/user/login", params=params)

    # Assert success response
    assert response.status_code == 200, f"Expected 200 but got {response.status_code}"
    assert "X-Rate-Limit" in response.headers, "Rate limit header missing"
    assert "X-Expires-After" in response.headers, "Expiration header missing"
    assert isinstance(response.json(), str), "Response body should be a string"

@pytest.mark.parametrize("credentials", invalid_credentials)
def test_login_invalid_credentials(api_client, credentials):
    """
    Test login with invalid credentials.
    """
    params = {"username": credentials['username'], "password": credentials['password']}
    response = api_client.get("/user/login", params=params)

    # Assert error response
    assert response.status_code == 400, f"Expected 400 but got {response.status_code}"
    assert response.json() == "Invalid username/password supplied"

def test_login_missing_credentials(api_client):
    """
    Test login request with missing credentials.
    """
    response = api_client.get("/user/login")

    # Assert error response
    assert response.status_code == 400, f"Expected 400 but got {response.status_code}"
    assert response.json() == "Invalid username/password supplied"

def test_login_unexpected_error(api_client, valid_api_key):
    """
    Test login handling unexpected server errors.
    """
    api_client.api_helper.base_url = "https://invalid.url.com"
    response = api_client.get("/user/login")

    # Assert default error response handling
    assert response.status_code != 200, "Expected error status code"
    assert response.json() != "successful operation", "Unexpected successful operation"

