# ********RoostGPT********

# Test generated by RoostGPT for test roost_test using AI Type Azure Open AI and AI Model gpt-4o
# 
# Test file generated for /pet/findByStatus_get for http method type GET 
# RoostTestHash=d31df25c68
# 
# 

# ********RoostGPT********
import pytest
from jsonschema import validate

# Test data schema for 200 response
SUCCESS_RESPONSE_SCHEMA = {
    "type": "array",
    "items": {
        "required": ["name", "photoUrls"],
        "type": "object",
        "properties": {
            "id": {"type": "integer", "format": "int64", "example": 10},
            "name": {"type": "string", "example": "doggie"},
            "category": {
                "type": "object",
                "properties": {
                    "id": {"type": "integer", "format": "int64", "example": 1},
                    "name": {"type": "string", "example": "Dogs"},
                },
            },
            "photoUrls": {
                "type": "array",
                "items": {"type": "string"},
            },
            "tags": {
                "type": "array",
                "items": {
                    "type": "object",
                    "properties": {
                        "id": {"type": "integer", "format": "int64"},
                        "name": {"type": "string"},
                    },
                },
            },
            "status": {"type": "string"},
        },
    },
}

@pytest.mark.parametrize("status", ["available", "pending", "sold"])
@pytest.mark.usefixtures("valid_api_key")
def test_find_pets_by_status_success(api_client, valid_api_key, status):
    """
    Test the /pet/findByStatus endpoint with valid statuses.
    """
    headers = {"Authorization": f"Bearer {valid_api_key}"}
    params = {"status": status}
    response = api_client.get("/pet/findByStatus", headers=headers, params=params)
    
    assert response.status_code == 200
    assert response.headers["Content-Type"] == "application/json"
    
    # Validate the response schema
    response_json = response.json()
    validate(instance=response_json, schema=SUCCESS_RESPONSE_SCHEMA)


def test_find_pets_by_status_invalid_status(api_client, valid_api_key):
    """
    Test the /pet/findByStatus endpoint with an invalid status.
    """
    headers = {"Authorization": f"Bearer {valid_api_key}"}
    params = {"status": "INVALID_STATUS"}
    response = api_client.get("/pet/findByStatus", headers=headers, params=params)
    
    assert response.status_code == 400
    assert "Invalid status value" in response.text


def test_find_pets_by_status_missing_auth(api_client):
    """
    Test the /pet/findByStatus endpoint without authentication.
    """
    params = {"status": "available"}
    response = api_client.get("/pet/findByStatus", params=params)
    
    assert response.status_code == 401
    assert "Unauthorized" in response.text


def test_find_pets_by_status_invalid_auth(api_client, invalid_api_key):
    """
    Test the /pet/findByStatus endpoint with an invalid API key.
    """
    headers = {"Authorization": f"Bearer {invalid_api_key}"}
    params = {"status": "available"}
    response = api_client.get("/pet/findByStatus", headers=headers, params=params)
    
    assert response.status_code == 401
    assert "Unauthorized" in response.text


def test_find_pets_by_status_unexpected_error(api_client, valid_api_key, mocker):
    """
    Test the /pet/findByStatus endpoint to simulate unexpected server error.
    """
    headers = {"Authorization": f"Bearer {valid_api_key}"}
    params = {"status": "available"}
    
    # Mock the API to return a 500 error for testing purposes
    mocker.patch.object(api_client.api_helper, "make_request", return_value=mocker.Mock(status_code=500))
    
    response = api_client.get("/pet/findByStatus", headers=headers, params=params)
    
    assert response.status_code == 500
    assert "Unexpected error" in response.text
