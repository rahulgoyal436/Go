# ********RoostGPT********

# Test generated by RoostGPT for test roost_test using AI Type Azure Open AI and AI Model gpt-4o
# 
# Test file generated for /pet/findByStatus_get for http method type GET 
# RoostTestHash=d31df25c68
# 
# 

# ********RoostGPT********
import pytest
import json
from jsonschema import validate

@pytest.mark.api
@pytest.mark.parametrize(
    "status_param, expected_status, expected_schema",
    [
        ("available", 200, {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "id": {"type": "integer"},
                    "name": {"type": "string"},
                    "category": {
                        "type": "object",
                        "properties": {
                            "id": {"type": "integer"},
                            "name": {"type": "string"},
                        }
                    },
                    "photoUrls": {"type": "array", "items": {"type": "string"}},
                    "tags": {"type": "array", "items": {
                        "type": "object",
                        "properties": {
                            "id": {"type": "integer"},
                            "name": {"type": "string"}
                        }
                    }},
                    "status": {"type": "string"},
                },
                "required": ["name", "photoUrls"]
            }
        }),
        ("sold", 200, None),  # Adjust schema validation as needed
        ("invalidstatus", 400, None),
    ])
@pytest.mark.parametrize("auth_type", ["valid_api_key", "oauth2_token", "invalid_api_key"])
def test_find_pets_by_status(api_client, config, auth_type, status_param, expected_status, expected_schema, valid_api_key, oauth2_token, invalid_api_key):
    """
    Test the /pet/findByStatus endpoint with different status values.
    Cover valid, invalid, and missing authentication scenarios.
    Validate responses against the OpenAPI spec.
    """

    # Prepare endpoint and headers
    endpoint = "pet/findByStatus"
    auth_header = {
        "Authorization": f"Bearer {oauth2_token}" if auth_type == "oauth2_token"
        else f"Api-Key {valid_api_key}" if auth_type == "valid_api_key"
        else f"Api-Key {invalid_api_key}"
    }

    # Make API call
    response = api_client.get(
        endpoint=endpoint,
        headers=auth_header,
        params={"status": status_param if status_param else None}
    )

    # Assert for the expected response status
    assert response.status_code == expected_status, f"Unexpected response status: {response.status_code}, Response content: {response.text}"

    # Response schema validation for successful operation
    if expected_status == 200 and expected_schema:
        try:
            response_json = response.json()
            validate(instance=response_json, schema=expected_schema)
        except json.JSONDecodeError:
            pytest.fail("Response is not valid JSON")
        except Exception as e:
            pytest.fail(f"Schema validation failed: {e}")
    elif expected_status == 400:
        assert "Invalid status value" in response.text, "Missing or invalid error message for 400 responses"
    elif response.status_code >= 500:
        pytest.fail(f"Server error encountered: {response.text}")
