# ********RoostGPT********

# Test generated by RoostGPT for test roost_test using AI Type Azure Open AI and AI Model gpt-4o
# 
# Test file generated for /project/{project-slug}/envvar/{name}_get for http method type GET 
# RoostTestHash=10e0229ba1
# 
# 

# ********RoostGPT********
import pytest
import requests

# Base URL fixture
@pytest.fixture(scope="module")
def base_url():
    return "https://circleci.com/api/v2"

# Authentication fixture
@pytest.fixture(scope="module")
def auth_headers():
    return {"Authorization": "Basic REPLACE_BASIC_AUTH"}

# Test Data
@pytest.fixture
def test_data():
    return {
        "valid_project_slug": "gh/CircleCI-Public/api-preview-docs",
        "valid_name": "foo",
        "invalid_project_slug": "invalid/project",
        "invalid_name": "invalid-env-var",
    }

# Helper function for endpoint URL
def get_endpoint(base_url, project_slug, name):
    return f"{base_url}/project/{project_slug}/envvar/{name}"

# Test for successful response
@pytest.mark.parametrize(
    "project_slug, name, expected_status",
    [
        ("gh/CircleCI-Public/api-preview-docs", "foo", 200),  # Valid case
    ],
)
def test_get_env_var_success(base_url, auth_headers, project_slug, name, expected_status):
    url = get_endpoint(base_url, project_slug, name)
    response = requests.get(url, headers=auth_headers)
    assert response.status_code == expected_status
    assert "name" in response.json()
    assert "value" in response.json()

# Test for invalid/missing parameters
@pytest.mark.parametrize(
    "project_slug, name, expected_status",
    [
        ("invalid/project", "foo", 400),  # Invalid project slug
        ("gh/CircleCI-Public/api-preview-docs", "", 404),  # Missing name
        ("", "foo", 404),  # Missing project slug
    ],
)
def test_get_env_var_error(base_url, auth_headers, project_slug, name, expected_status):
    url = get_endpoint(base_url, project_slug, name)
    response = requests.get(url, headers=auth_headers)
    assert response.status_code == expected_status

# Test for authentication scenarios
@pytest.mark.parametrize(
    "auth_headers, expected_status",
    [
        ({"Authorization": "Basic VALID_AUTH"}, 200),  # Valid authentication
        (None, 401),  # Missing authentication
        ({"Authorization": "Basic INVALID_AUTH"}, 403),  # Invalid authentication
    ],
)
def test_get_env_var_auth(base_url, auth_headers, test_data, expected_status):
    url = get_endpoint(base_url, test_data["valid_project_slug"], test_data["valid_name"])
    response = requests.get(url, headers=auth_headers)
    assert response.status_code == expected_status

# Schema validation test
def test_get_env_var_schema(base_url, auth_headers, test_data):
    url = get_endpoint(base_url, test_data["valid_project_slug"], test_data["valid_name"])
    response = requests.get(url, headers=auth_headers)
    assert response.status_code == 200
    data = response.json()
    assert "name" in data and isinstance(data["name"], str)
    assert "value" in data and isinstance(data["value"], str)
    if "created-at" in data:
        assert isinstance(data["created-at"], str)

