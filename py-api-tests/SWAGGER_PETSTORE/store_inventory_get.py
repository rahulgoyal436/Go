# ********RoostGPT********

# Test generated by RoostGPT for test roost_test using AI Type Azure Open AI and AI Model gpt-4o
# 
# Test file generated for /store/inventory_get for http method type GET 
# RoostTestHash=a66f7079af
# 
# 

# ********RoostGPT********
import pytest
import requests
import yaml

# Load configuration from YAML
@pytest.fixture(scope="session")
def config():
    with open("config.yml", "r") as f:
        return yaml.safe_load(f)

# Fixture for base URL from config
@pytest.fixture(scope="session")
def base_url(config):
    return config["api"]["host"]

# Fixture for authentication headers
@pytest.fixture(scope="session")
def auth_headers(config):
    return {"api_key": config["authentication"]["api_key"]}

# Test for the /v2/store/inventory endpoint
class TestInventoryEndpoint:
    @pytest.mark.parametrize(
        "expected_status_code, description",
        [
            (200, "successful operation"),
        ]
    )
    def test_get_inventory_success(self, base_url, auth_headers, expected_status_code, description):
        """
        Test retrieving pet inventories by status with valid authentication.
        """
        url = f"{base_url}/store/inventory"
        response = requests.get(url, headers=auth_headers)

        # Assert status code
        assert response.status_code == expected_status_code, f"Failed on: {description}"

        # Assert response is a JSON object
        assert response.headers["Content-Type"] == "application/json"
        
        # Assert response matches schema (a map of string keys to integer values)
        inventory = response.json()
        assert isinstance(inventory, dict)
        for key, value in inventory.items():
            assert isinstance(key, str)
            assert isinstance(value, int)

    def test_get_inventory_unauthorized(self, base_url):
        """
        Test retrieving pet inventories without authentication (unauthorized).
        """
        url = f"{base_url}/store/inventory"
        response = requests.get(url)

        # Assert 401 Unauthorized
        assert response.status_code == 401

    def test_get_inventory_invalid_key(self, base_url):
        """
        Test retrieving pet inventories with an invalid API key.
        """
        url = f"{base_url}/store/inventory"
        invalid_headers = {"api_key": "invalid-key"}
        response = requests.get(url, headers=invalid_headers)

        # Assert 403 Forbidden or similar
        assert response.status_code in [401, 403]

